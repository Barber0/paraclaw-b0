#!/bin/bash
# Git Worktree Session Manager - 便捷入口
# 用法: worktree-session <command> [args...]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "$SCRIPT_DIR/worktree_session.py" "$@"
